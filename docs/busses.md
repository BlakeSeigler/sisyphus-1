Wire Busses:

- generally pulled to a level
- ports typically have tri state buffers and maintain a high impedance state until ready to run
- states mainained by a push-pull configuration
- there is typically a bus master that triggers the different ports to activate or deactivate from the high impedance state
    > these can be memory controllers, cpus, or other